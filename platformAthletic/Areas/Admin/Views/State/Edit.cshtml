@model platformAthletic.Models.ViewModels.StateView
@{
    ViewBag.Title = Model.ID == 0 ? "Add State" : "Edit State";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
<h2>@(Model.ID == 0 ? "Add State" : "Edit State")</h2>
<p></p>
@using (Html.BeginForm("Edit", "State", FormMethod.Post, new { @class = "form-horizontal" }))
{
	<fieldset>
		@Html.Hidden("ID", Model.ID)

		<div class="control-group">
            <label class="control-label" for="Name">
                Name
			</label>
            <div class="controls">
                @Html.ValidationMessage("Name")
				@Html.TextBox("Name", Model.Name)
            </div>
        </div>
		<div class="control-group">
            <label class="control-label" for="Code">
                Code
			</label>
            <div class="controls">
                @Html.ValidationMessage("Code")
				@Html.TextBox("Code", Model.Code)
            </div>
        </div>
	   <div class="form-actions">
            <button type="submit" class="btn btn-primary">
                Save
			</button>
            @Html.ActionLink("Cancel", "Index", null, null, new { @class = "btn" })
        </div>
	</fieldset>
}
