@model platformAthletic.Models.ViewModels.FeatureTextView
@{
    ViewBag.Title = Model.ID == 0 ? "Add Feature train" : "Edit Feature train";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
@section header {
    <script type="text/javascript" src="/Media/scripts/admin/file-upload.js"></script>
}
<h2>@(Model.ID == 0 ? "Add Feature train" : "Edit Feature train")</h2>
<p></p>
@using (Html.BeginForm("EditText", "Feature", FormMethod.Post, new { @class = "form-horizontal" }))
{
	<fieldset>
		@Html.Hidden("_ID", Model._ID)
		@Html.Hidden("FeatureCatalogID", Model.FeatureCatalogID)

		<div class="control-group">
            <label class="control-label" for="Header">
                Header
			</label>
            <div class="controls">
                @Html.ValidationMessage("Header")
				@Html.TextBox("Header", Model.Header)
            </div>
        </div>
		<div class="control-group">
            <label class="control-label" for="Text">
                Text
			</label>
            <div class="controls">
                @Html.ValidationMessage("Text")
				@Html.TextArea("Text", Model.Text, new {@class = "htmltext"})
            </div>
        </div>
        <div class="control-group">
            <label class="control-label" for="Text">
                Images
            </label>
            <div id="UploadImage">
                Upload 
            </div>
        </div>
        <ul class="thumbnails">
            @Html.Action("Index", "File", new { page = 1 })
        </ul>
	   <div class="form-actions">
            <button type="submit" class="btn btn-primary">
                Save
			</button>
            @Html.ActionLink("Cancel", "Index", null, null, new { @class = "btn" })
        </div>
	</fieldset>
}
