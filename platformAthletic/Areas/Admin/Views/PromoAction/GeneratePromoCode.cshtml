@model platformAthletic.Models.ViewModels.PromoCodeView
@using (Html.BeginForm("GeneratePromoCode", "PromoAction", FormMethod.Post, new { id = "GeneratePromoCodeForm", @class = "form-horizontal" }))
{ 
    <div class="modal-body">
        <fieldset>
            @Html.Hidden("ID", Model.ID)
            @Html.Hidden("PromoActionID", Model.PromoActionID)
            <div class="control-group">
                <label class="control-label" for="ReferralCode">
                    Referral Code
                </label>
                <div class="controls">
                    @Html.TextBox("ReferralCode", Model.ReferralCode)
                    @Html.ValidationMessage("ReferralCode")
                    
                </div>
            </div>
            <div class="control-group">
                <label class="control-label" for="Reusable">
                    Reusable
                </label>
                <div class="controls">
                    @Html.CheckBox("Reusable", Model.Reusable)
                </div>
            </div>
             <div class="control-group">
                <label class="control-label" for="Quantity">
                    Quantity
                </label>
                <div class="controls">
                    @Html.TextBox("Quantity", Model.Quantity)
                    @Html.ValidationMessage("Quantity")
                </div>
            </div>
        </fieldset>
    </div>
    <div class="modal-footer">
        <button class="btn btn-primary" id="GenerateButton">Generate</button>
        <button class="btn" data-dismiss="modal" aria-hidden="true">Cancel</button>
    </div>    
}
