@model AssistantUserView
<div class="modal fade" id="modalRemoveAssistant" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title" id="myModalLabel">REMOVE COACH</h4>
            </div>
            <div class="modal-body">
                <div id="ModalEditAssistantWrapper">
                    @using (Html.BeginForm("Delete", "ManageAssistant", FormMethod.Post, new { id = "AssistantForm" }))
                    {
                        @Html.Hidden("ID", Model.ID)
                    }
                    <div class="row">
                        <p>Do you want to delete @Model.FirstName @Model.LastName?</p>
                    </div>
                    <div class="modal-footer text-center">
                        <button type="button" class="btn btn-default" data-dismiss="modal">CANCEL</button>
                        <button type="button" class="btn btn-primary" id="SubmitRemoveAssistant">Remove</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>