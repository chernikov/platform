@model IEnumerable<platformAthletic.Model.Page>

@{
    ViewBag.Title = "Page";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
<h2>
	Page
</h2>
@Html.ActionLink("Create", "Create", "Page", null, new { @class = "btn" })
<table class="table">
	<thead>
		<tr>
			<th>
					#
			</th>
			<th>
				Name
			</th>
			<th>
			</th>
		</tr>
	</thead>
	
	@foreach(var item in Model) {
	<tr>
		<td> 
			@item.ID
		</td>
		<td> 
			@item.Name
		</td>		
		<td>
			@Html.ActionLink("Edit", "Edit", "Page", new { id = item.ID }, new { @class = "btn" })
            @Html.ActionLink("Remove", "Delete", "Page", new { id = item.ID }, new { @class = "btn" })
		</td>
	</tr>
	}
</table>
